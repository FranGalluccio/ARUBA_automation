name: Python Playwright Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: self-hosted
    # labels: [ windows ]

    steps:
      # 1️⃣ Checkout del repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Python
      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      # 3️⃣ Install dependencies e browser Playwright
      - name: Install dependencies
        shell: powershell
        run: |
          pip install -r requirements.txt
          pip install pytest-playwright pytest-html
          playwright install

      # 4️⃣ Esegui test generando report HTML e XML
      - name: Run tests
        shell: powershell
        continue-on-error: true
        run: |
          $reportDir = "pytest_playwright/test-results"
          if (!(Test-Path $reportDir)) { New-Item -ItemType Directory -Path $reportDir }

          pytest pytest_playwright/ `
            --junitxml=$reportDir/report.xml `
            --html=$reportDir/report.html `
            --self-contained-html `
            --capture=tee-sys

      # 5️⃣ Carica report XML
      - name: Upload JUnit XML report
        uses: actions/upload-artifact@v4
        with:
          name: junit-report
          path: pytest_playwright/test-results/report.xml

      # 6️⃣ Carica report HTML
      - name: Upload HTML report
        uses: actions/upload-artifact@v4
        with:
          name: html-report
          path: pytest_playwright/test-results/report.html

      # 7️⃣ Carica tutti gli screenshot
      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: pytest_playwright/test-results/**/*.png
